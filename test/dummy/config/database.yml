common: &common
  adapter: postgresql
  encoding: unicode
  username: <%= `whoami` %>
  host: 127.0.0.1
  password:

development:
  database: full-and-mountable_development
  min_messages: warning
  <<: *common

test:
  database: full-and-mountable_test
  min_messages: warning
  <<: *common

